import { Server } from 'socket.io';
import { GameService } from 'src/game/game.service';
import UsersSockets from 'src/gateway/socket.class';
import { UserDto } from 'src/user/dto/user.dto';
export declare class GameRoom {
    private playerL;
    private playerR;
    private interval;
    private winners;
    private status;
    private ballSpeedX;
    private ballSpeedY;
    private ball;
    private rackets_ypos;
    private rackets_acel;
    private server;
    private room;
    roomN: number;
    private gameService;
    userSockets: UsersSockets;
    isrunning: boolean;
    constructor(server: Server, roomN: number, gameService: GameService, userSockets: UsersSockets);
    private emit2all;
    init(player: UserDto): void;
    setPlayers(playerR: UserDto, playerL: UserDto): void;
    checkPlayer(player: UserDto): boolean;
    private updatePositions;
    initMoveEvents(): void;
    private save_results2DB;
    private destroy_game;
    run(): void;
    exitGame(player: UserDto): void;
}
